# === SEGREDOS — nunca versionar ===
mojinho-bot/token
# legado: token do Telegram hardcoded — versione só mojinho-api.sh
mojinho-bot/mojinho.sh
# config real do bot (versione só bot.conf.sample)
mojinho-bot/*.conf
secrets/
*.pem
*.key
id_rsa*
.env

# === estado de runtime (ancorado na raiz; não pegar dirs aninhados homônimos) ===
/run/
*.sock
*.pid
*.log

# === dados: fonte da verdade, fora do versionamento (têm senhas/submissões) ===
/contests/

# === legado e repositórios com identidade própria (ancorados na raiz) ===
/old/
/mojtools/
/judge/

# === logs/estado do bot ===
mojinho-bot/log-*.txt
mojinho-bot/mojinho-offset

# === docs HTML gerado (rode docs/build-html.sh) ===
/docs/html/

# === diversos ===
*.swp
*~
.DS_Store
node_modules/

# CLI de autoria servido na raiz web (origem: ../moj-cli/moj — copiado pelo setup.sh)
/web/moj
